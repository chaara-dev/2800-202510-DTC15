<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Reminders</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <link
    href="https://fonts.googleapis.com/css2?family=Delius+Swash+Caps&family=Emilys+Candy&display=swap"
    rel="stylesheet">
  <link href="../CSS/contact.css" rel="stylesheet">
  <link rel="stylesheet" href="../CSS/style_footer.css">
  <link rel="stylesheet" href="../CSS/main_style.css">
  <link rel="stylesheet" href="../CSS/style_reminders.css">

  <style>
    html, body {
      margin: 0;
      padding: 0;
      height: 100%;
    }

    body {
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    .main-content {
      flex: 1; 
    }

    .navbar, .footer {
      margin: 0;
    }
  </style>
</head>

<body class="backimage">
  <nav class="navbar flex flex-col md:flex-row md:items-center md:justify-between p-4 bg-[#0C4A2Cd4] text-[#F9F9F6]">
    <div class="flex items-center justify-between w-full md:w-auto">
      <div class="text-2xl font-bold flex items-center">
        <i class="fas fa-leaf mr-2 text-white"></i>
        <span class="nav-brand">PlantPal</span>
      </div>
      <button class="menu-toggle md:hidden text-3xl" id="menu-toggle">&#9776;</button>
    </div>
    <div class="nav-links" id="nav-links">
      <a href="/home">Home</a>
      <a href="/HTML/reminders.html">Reminders</a>
      <a href="/HTML/location.html">Location</a>
      <a href="/HTML/calendar.html">Calendar</a>
      <a href="/HTML/settings.html">Settings</a>
      <div class="logout-button">
        <a href="/logout">Logout</a>
      </div>
    </div>
  </nav>

  <div class="main-content px-6 py-8 max-w-6xl mx-auto">
    <div class="content-box">
      <h1 class="large-heading">Welcome, <span class="highlight"><%= username %></span></h1>
      <p class="text-white font-medium mt-2">Your plant collection:</p>

      <% if (userPlants.length === 0) { %>
        <p class="text-white mt-4">You haven't added any plants yet!</p>
      <% } %>

      <div class="mt-4 mb-6">
        <a href="/addplant"
          class="inline-block bg-emerald-600 hover:bg-emerald-700 text-white font-semibold py-2 px-4 rounded-lg shadow-md transition duration-200">
          Add More Plants
        </a>
      </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      <% userPlants.forEach(plant => { %>
        <div class="bg-[#fcf1e1] rounded-2xl shadow-md p-4 text-[#0C4A2C] relative">
          <% if (plant.image && plant.image !== "—") { %>
            <img src="<%= plant.image %>" alt="<%= plant.name %>" class="w-full h-48 object-cover rounded-xl mb-3 shadow" />
          <% } else { %>
            <div class="w-full h-48 flex items-center justify-center bg-[#eee0cb] text-[#c2847a] rounded-xl mb-3 text-sm">
              No Image Added
            </div>
          <% } %>

          <h2 class="text-xl font-bold mb-1"><%= plant.name %></h2>
          <p class="italic text-[#c2847a] mb-2"><%= plant.scientific_name || '—' %></p>
          <p><strong>☀️ Sunlight:</strong> <%= plant.sunlight || '—' %></p>
          <p><strong>💧 Watering:</strong> <%= plant.watering || '—' %></p>
          <p><strong>⏳ Duration:</strong> <%= plant.duration || '—' %></p>

          <form method="POST" action="/deleteplant/<%= plant._id %>" class="absolute top-3 right-3">
            <button class="bg-[#c2847a] hover:bg-[#a96d60] text-white px-3 py-1 rounded-lg text-xs shadow transition">
              Delete
            </button>
          </form>
        </div>
      <% }) %>
    </div>

    </div>
  </div>

  <footer class="footer bg-[#0C4A2Cd4]">
    <div class="footer-brand">
      <svg height="32px" width="32px" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512.001 512.001" fill="#000000">
        <path style="fill:#6DC180;" d="..."></path>
        <path style="fill:#5CA15D;" d="..."></path>
        <polygon style="fill:#4E894D;" points="..."></polygon>
      </svg>
      <p class="footer-title">PlantPal</p>
    </div>
    <div class="footer-links">
      <a href="/home"><img src="../Resources/Icons/home.png" alt="home" class="footer-icon"></a>
      <a href="/about"><img src="../Resources/Icons/info.png" alt="about" class="footer-icon"></a>
      <a href="/contact"><img src="../Resources/Icons/contact-us.png" alt="contact" class="footer-icon"></a>
    </div>
    <p class="footer-copy">&copy; 2025 PlantPal. All rights reserved.</p>
  </footer>

  <script src="/scripts/chatbot.js"></script>
  <script src="/scripts/script.js"></script>
</body>

</html>
